apiVersion: v1
kind: Service
metadata:
  labels:
    app: greeter-server
  name: greeter-server
  namespace: default
spec:
  ports:
    - name: grpc
      port: 50051
      protocol: TCP
      targetPort: 50051
  selector:
    app: greeter-server
  sessionAffinity: None
  type: ClusterIP
